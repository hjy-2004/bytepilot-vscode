name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release-vscode:
    name: Release VS Code Extension
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - name: Build core
        run: npm run build:core
      - name: Build extension
        run: node esbuild.config.mjs
      - name: Build webview (VS Code theme)
        run: cd webview-ui && npm ci && npm run build
      - name: Package VSIX
        run: npx vsce package --no-dependencies
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: '*.vsix'
          generate_release_notes: true

  release-desktop:
    name: Release Desktop (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-x64
            target: x86_64-unknown-linux-gnu
            file: bytepilot_*_amd64.deb
          - os: windows-latest
            name: windows-x64
            target: x86_64-pc-windows-msvc
            file: BytePilot_*_x64-setup.exe
          - os: macos-latest
            name: macos-arm64
            target: aarch64-apple-darwin
            file: BytePilot_*_aarch64.dmg
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - run: npm ci
      - name: Install webview UI dependencies
        run: cd webview-ui && npm ci
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - name: Build Tauri app
        run: cd packages/tauri-app && npm ci && npx tauri build --target ${{ matrix.target }}
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          files: packages/tauri-app/src-tauri/target/release/bundle/${{ matrix.file }}
          generate_release_notes: true
