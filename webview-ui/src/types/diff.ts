/**
 * Diff data structures shared between extension host and webview.
 * Now sourced from @bytepilot/core — single source of truth.
 */
export type { UnifiedDiff, DiffHunk, DiffLine } from '@bytepilot/core';
